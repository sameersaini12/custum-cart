<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/app.css">
    <title>Custum Cart</title>
    <style>
        body {
            margin: 0;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen',
            'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans', 'Helvetica Neue',
            sans-serif;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
            background-color:rgb(234,237,237);
        }
    </style>
</head>
<body>
    
    <nav class="header">
        <a style="color:white; text-decoration:none; margin: 0 10px;" href="/">
          Custum Cart
        </a>
        <div class="header_search">
          <input type="text" class="header_searchInput" />
        </div>
        <div class="headerNav">
          <div class="header_link" href="/login">
            <div class="header_option">
            <span class="header_optionLineOne">Hello <% if(user) { %>  <%= user.name %> <% } else {%> User <% } %></span>
              <span class="header_optionLineTwo">
                <% if(user) {  %> 
                  <form id="logout" action="/logout" method="POST">
                  <a onclick="document.getElementById('logout').submit() " style="text-decoration: none; color: white;" href="#">Sign out </a>
                </form>
                  <% } else { %>  <a style="text-decoration: none; color: white;" href="/login" >Sign In </a><% } %>
              </span>
            </div>
          </div>
          <a class="header_link" href="/customers/orders">
            <div class="header_option">
              <span class="header_optionLineOne">Returns</span>
              <span class="header_optionLineTwo">& Orders</span>
            </div>
        </a>
          <a class="header_link" href="/">
            <div class="header_option">
              <span class="header_optionLineOne">Your</span>
              <span class="header_optionLineTwo">Prime</span>
            </div>
        </a>
  
          <a class="header_link" href="/cart">
            <div class="header_optionBasket">
              <img src="/img/cart.png" alt="" />
              <span class="header_optionLineTwo header_basketCount" id="cartCounter">
                <%= session.cart ? session.cart.totalQty : '' %>
              </span>
            </div>
          </a>
        </div>
      </nav>
      
    <%- body %>

    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/app.js"></script>
</body>
</html>